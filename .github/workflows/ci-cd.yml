name: CI/CD Pipline

on:
  push:
    branches: [ main,master ]
  pull_request:
    branches: [ main,master ]

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
    - name: 检出代码
      uses: actions/checkout@v4

     - name: 设置Python环境
      uses: actions/setup-python@v4
      with:
        python-version: '3.11'

    - name: 安装依赖
      run: |
        pip install -r requirements.txt
        pip install flake8 pytest

    - name: 代码语法检查
      run: |
        flake8 . --count --select=E9,F63,F7,F82 --show-source --statistics

    - name: 运行测试
      run: |
        python -c "import flask; print('Flask版本:', flask.__version__)"
        echo "测试通过！"

  build:
    runs-on: ubuntu-latest
    needs: test

    steps:
    - name: 检出代码
      uses: actions/checkout@v4

    - name: 构建Docker镜像
      run: |
        docker build -t my-devops-app:latest .

    - name: 测试Docker镜像
      run: |
        docker run --rm my-devops-app:latest python -c "import flask; print('Docker镜像中的Flask版本:', flask.__version__)"

    - name: 上传构建成功状态
      run: echo "🎉 构建成功！镜像已就绪。"
