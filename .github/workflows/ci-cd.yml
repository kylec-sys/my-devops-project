name: CI/CD Pipeline

on:
  push:
    branches: [ main, master ]
  pull_request:
    branches: [ main, master ]

jobs:
  test:
    runs-on: ubuntu-latest
    
    steps:
    - name: 检出代码
      uses: actions/checkout@v4
      
    - name: 设置Python环境
      uses: actions/setup-python@v4
      with:
        python-version: '3.11'
        
    - name: 安装依赖
      run: |
        pip install -r requirements.txt
        
    - name: 代码质量检查
      run: |
        flake8 . --count --max-complexity=10 --statistics
        
    - name: 运行单元测试
      run: |
        python -m pytest test_app.py -v
        
  build:
    runs-on: ubuntu-latest
    needs: test
    
    steps:
    - name: 检出代码
      uses: actions/checkout@v4
      
    - name: 设置Docker构建环境
      run: |
        sudo apt-get update
        sudo apt-get install -y docker.io
        
    - name: 构建Docker镜像
      run: |
        docker build -t my-devops-app:latest .
        
    - name: 测试Docker容器
      run: |
        # 测试容器能否正常启动
        docker run -d --name test-container -p 5001:5000 my-devops-app:latest
        sleep 5
        docker ps | grep test-container
        curl -f http://localhost:5001/health || echo "健康检查失败"
        docker stop test-container
        docker rm test-container
        
    - name: 上传构建成功状态
      run: echo "🎉 CI/CD流水线执行成功！"
